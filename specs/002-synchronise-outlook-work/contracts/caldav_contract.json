{
  "name": "caldav_event_contract",
  "description": "Minimal contract for CalDAV event create/update operations used by sync tool",
  "endpoints": [
    {
      "method": "PUT",
      "path": "/calendars/{user}/{calendar}/{uid}.ics",
      "required_fields": ["UID","DTSTART","DTEND","SUMMARY"]
    }
  ]
}
